(* @authors Eric Han (eh636), Sebastian Jean-Francois (sj728), Sriram Murali
   (ssm238), Varun Gande (vg262) *)

let program () =
  let () = print_string "Welcome to OCamlJack! Enter START to begin: " in
  let _ = read_line () in
  ()

let () = program ()

(* Get card information from player and dealer (number and suit of each open
   card) *)

(* Get card information of player after they make a move *)

(* Get card information of everyone at end of game AND who won/lost *)
